name: flutter_example
description: A Flutter example for kiwi
version: 1.0.0+1

environment:
  sdk: ">=2.2.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  kiwi:
    path: ../kiwi/

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.10.3
  kiwi_generator:
    path: ../kiwi_generator/

flutter:
  uses-material-design: true